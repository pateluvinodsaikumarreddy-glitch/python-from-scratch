name="VINODSAI"
print(name[0:3])

print(name[0:])

print(name[:5])

print(name[:]) #Here the first place is gonna be the start that is 0 and the last is len(string)

print(name[0:len(name)]) #length is a function we use with arrays to get the length of the array how many elements are there in that array 